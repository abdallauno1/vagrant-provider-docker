Vagrant.configure("2") do |config|
  config.vm.provider "docker" do |d|
    d.image = "abdallauno1/resumeapp:v1"
  end
end

=begin
# start comment 

Vagrant.configure("2") do |config|
  config.vm.provider "docker" do |d|
    d.build_dir = "."
  end
end

Vagrant.configure("2") do |config|
  config.vm.provider "docker" do |d|
    d.vagrant_vagrantfile = "../path/to/Vagrantfile"
  end
end

# end comment
=end